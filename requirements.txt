# SecOpsPulse Python requirements.
# These packages support Ansible execution, Jinja template rendering, and common automation helpers.
# On Ubuntu/WSL, installing Ansible through apt is usually enough; this file is useful for pip-based setups.
# Keep versions broad so the project works across common Ansible 2.x environments.
ansible>=2.9.0,<6.0.0
jinja2>=2.10.0
PyYAML>=5.1.0
netaddr>=0.8.0
requests>=2.25.0